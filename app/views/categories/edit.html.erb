<h1 class="text-center mt-4">Edit category name</h1>

<%= render 'shared/errors', obj: @category %>
<div class="row justify-content-center">
  <div class="col-6">
    <%= render 'form' %>
  </div>
  <!--<div class="mb-3">
    <%= link_to '[ Cancel and return to category ]', category_path(@category)%>
  </div> * 중앙으로 배열되지 않는 문제가 생김 *--> 
</div> 

